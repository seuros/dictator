# Blog Post Without Frontmatter

This markdown file has no YAML frontmatter at all.
It starts directly with content.

The decree.frontmatter should ignore this file since there's no frontmatter to validate.
No `---` markers present.
